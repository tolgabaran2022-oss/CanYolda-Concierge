# Exclude workspace-level node_modules (pnpm virtual store is huge)
node_modules/

# Exclude other artifacts — not needed for mobile native build
artifacts/api-server/
artifacts/pitch-deck/
artifacts/pitch-deck-light/
artifacts/mockup-sandbox/

# Exclude docs and agent memory
docs/
.agents/
.local/

# Exclude build outputs and logs
dist/
*.log
*.tsbuildinfo
.expo/
android/
ios/
